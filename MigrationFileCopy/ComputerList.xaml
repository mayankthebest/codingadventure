<UserControl x:Class="MigrationFileCopy.ComputerList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="25*"/>
            <RowDefinition Height="275*"/>
        </Grid.RowDefinitions>
        <TextBox Text="{Binding ComputerList}" Grid.Row="1" HorizontalAlignment="Stretch" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Visible" Grid.RowSpan="2"/>
        <Label Content="Enter each Remote Computer Name on a separate line" Grid.Row="0" HorizontalAlignment="Left" Margin="0" VerticalAlignment="Center"/>
    </Grid>
</UserControl>
